# Leave unset for local dev — Vite's proxy in vite.config.js forwards /api to your local backend.
# For production (Vercel), set this to your deployed Render backend URL, including /api:
# VITE_API_URL=https://your-backend-name.onrender.com/api
VITE_API_URL=
